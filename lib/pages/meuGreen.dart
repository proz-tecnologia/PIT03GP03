import 'package:flutter/material.dart';

class MeuGreen extends StatelessWidget {
  const MeuGreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Meu Green'),
        centerTitle: true,
      ),

    );
  }
}